version: 2

sources:
  - name: bronze
    database: "{{ env_var('GCP_PROJECT_ID') }}"
    schema: bronze
    tables:
      - name: games
      - name: leagues
      - name: players
      - name: seasons
      - name: standings
      - name: teams